{
  "policies": {
    "clientEvents": "07 §8 row 4 — POST /api/events. No such route exists in the London tree; the client-event intake is not a Phase 1 surface. Wire the policy with the route.",
    "cookieConsent": "07 §8 row 5 — POST /api/legal/cookie-consent. The only route at that path is the legacy Sydney one from the bootstrap import (src/lib/supabase/*), which F-d rewrites; there is no London surface to consume it yet.",
    "katieChat": "07 §8 row 8 — Katie chat. The London Katie routes are not built; /api/chat/* is the legacy Sydney tree (F-d). The chat_cost_daily USD cap is the hard stop until then.",
    "katieUploads": "07 §8 row 8 — Katie uploads. Same surface as katieChat: not built in the London tree.",
    "agentRoute": "07 §8 row 9 — the ADMIN_API_TOKEN recruitment-agent route. /api/agent is the legacy Sydney route (F-d); the London one does not exist.",
    "nannyApplications": "07 §8 row 12 — nanny applications on the self-serve path. No Phase 1 surface lets a nanny apply; connections' K rows are the admin path and carry their own preconditions.",
    "adminRoutes": "07 §8 row 14 — admin routes. Phase 1 added no London admin route: the one admin surface is a page (src/app/admin/calls/page.tsx) that gates itself with requireRole(\"admin\") and notFound()s. Wire the policy with the first admin route."
  },
  "actions": {
    "src/app/admin/katie-proposals/actions.ts": "Legacy Sydney admin actions from the bootstrap import (they build on @/lib/supabase/*) and are rewritten by F-d; 07 §8 row 14's adminRoutes is itself recorded as unwired.",
    "src/app/admin/leads/actions.ts": "Legacy Sydney admin actions from the bootstrap import (they build on @/lib/supabase/*) and are rewritten by F-d; 07 §8 row 14's adminRoutes is itself recorded as unwired.",
    "src/app/admin/positions/actions.ts": "Legacy Sydney admin actions from the bootstrap import (they build on @/lib/supabase/*) and are rewritten by F-d; 07 §8 row 14's adminRoutes is itself recorded as unwired.",
    "src/modules/admin/calendar/actions/block-range-action.ts": "07 §8 row 14 — admin-gated by auth.requireRole('admin') + aal2 (07 §5.4 rows 1–2); adminRoutes is recorded as unwired in \"policies\" above and the five London admin actions take it together.",
    "src/modules/admin/call-queue/actions/book-call-slot-action.ts": "07 §8 row 14 — admin-gated by auth.requireRole('admin') + aal2 (07 §5.4 rows 1–2); adminRoutes is recorded as unwired in \"policies\" above and the five London admin actions take it together.",
    "src/modules/admin/call-queue/actions/clear-call-slot-action.ts": "07 §8 row 14 — admin-gated by auth.requireRole('admin') + aal2 (07 §5.4 rows 1–2); adminRoutes is recorded as unwired in \"policies\" above and the five London admin actions take it together.",
    "src/modules/admin/call-queue/actions/move-call-slot-action.ts": "07 §8 row 14 — admin-gated by auth.requireRole('admin') + aal2 (07 §5.4 rows 1–2); adminRoutes is recorded as unwired in \"policies\" above and the five London admin actions take it together.",
    "src/modules/admin/call-queue/actions/record-call-outcome-action.ts": "07 §8 row 14 — admin-gated by auth.requireRole('admin') + aal2 (07 §5.4 rows 1–2); adminRoutes is recorded as unwired in \"policies\" above and the five London admin actions take it together.",
    "src/modules/app/child-linking/actions/create-child-action.ts": "No 07 §8 row: `07.50`'s add-a-child is S-N-01's sibling on the parent side and row 17 was written for the nanny one — recorded for a per-user ceiling of the same shape, owner 07 §8 / `1i`.",
    "src/modules/app/child-linking/actions/create-child-invite-action.ts": "The mint is idempotent per child (02 §4.6 — no rotation), so a repeat call hands back the link that exists rather than writing a second; recorded with create-child for a row 17-shaped ceiling, owner 07 §8 / `1i`.",
    "src/modules/app/child-linking/actions/revoke-child-invite-action.ts": "Terminal write on a row the caller already holds, with no un-revoke and no rotation, so a loop mints nothing; recorded with create-child for a row 17-shaped ceiling, owner 07 §8 / `1i`.",
    "src/modules/app/child-linking/actions/start-invite-signup-action.ts": "Writes no row — it normalises the token and sets ADR-150's HttpOnly cookie; 07 §8 row 7's lookup budget is consumed by the preview and the claim that follow it.",
    "src/modules/auth/actions/set-password-action.ts": "07 §8 row 3 names set-password, and this boundary is reached only with a live session (ADR-042) whose link is single-use and 1 h while Supabase Auth's own limits stay on — recorded for the row-3 consume here, owner 07 §8 / `auth`.",
    "src/modules/call-layer/actions/list-nanny-slots-action.ts": "A read, not a write — S-N-02's calendar re-read (04 §4.4 c3); 07 §8 row 1's budget for reads is the edge's.",
    "src/modules/call-layer/actions/list-slots-action.ts": "A read, not a write — S-P-02's slot re-read for the signed-in parent's open call; 07 §8 row 1's budget for reads is the edge's.",
    "src/modules/connections/actions/connect-to-nanny-action.ts": "07 §8 row 12 — connection requests; `nannyApplications` is recorded as unwired above and row 12's other half, ≤ config pending per parent, is enforced by K-1's own precondition meanwhile.",
    "src/modules/matching/actions/connect-action.ts": "07 §8 row 12 — the same row and the same precondition as connect-to-nanny (ADR-126 makes this the one entry point `public-site` calls).",
    "src/modules/onboarding-parent/actions/create-position-action.ts": "No 07 §8 row for the in-app position: row 9's ≤ 200/day is the agent route's — recorded for a row of its own, owner 07 §8 / `onboarding-parent`.",
    "src/modules/positions/actions/close-position-action.ts": "A parent's one lever on her own position (03 §2.4 P-7), gated by `advance`'s party check; no 07 §8 row — recorded with create-position, owner 07 §8 / `positions`.",
    "src/modules/verification/actions/submit-identity-action.ts": "07 §8 row 11 is consumed one boundary in, in verification/lib/submit-identity.ts — the action delegates through VERIFICATION_REGISTRY, which no static read crosses.",
    "src/modules/verification/actions/submit-dbs-action.ts": "07 §8 row 11 is consumed one boundary in, in verification/lib/submit-dbs.ts — the action delegates through VERIFICATION_REGISTRY, which no static read crosses.",
    "src/modules/verification/actions/submit-right-to-work-action.ts": "07 §8 row 11 is consumed one boundary in, in verification/lib/submit-right-to-work.ts — the action delegates through VERIFICATION_REGISTRY, which no static read crosses."
  }
}
