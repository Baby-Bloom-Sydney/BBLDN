// admin/users — the panel's own type surface (01 §2.5).
//
// GAP — recorded in the L-005 F-c PROGRESS entry: 03 §10.1 lists the connector methods `admin` calls as one set
// for the whole module, and no section splits them per panel or states a panel's read signature. The descriptor
// in `panel.ts` carries what the foundations do state — the panel's name, route and screen IDs (04 §6.4).
export type { AdminPanel, AdminPanelName } from "../types";
