// S-P-01's route (04 §2.2) — where S-P-04 sends a parent once the position is open and P-2's cascade has opened
// the call. Paths are app structure, not config (05 §6); this is the one place the in-app flow names it, beside
// `postSignupDestination`'s copy for path B.
export const PARENT_CALL_PATH = "/parent/call";
