// Lifted to `platform` by `2a` (the nanny surfaces ask the same number under the same rule and 01 §2.3 gives
// `onboarding-nanny` no arrow here). Re-exported so this module's connector and its suites keep their name.
export { normaliseUkMobile } from "@/modules/platform";
