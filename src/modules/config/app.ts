// 01 §3.1 — the app's product values (02 §4.6, §9 item 5; 07 §6.2 row 8).
export const APP = Object.freeze({
  maxChildAgeMonths: 36, // @pending:B-15 (product rule vs prototype artefact; default running now)
  frameworkLabel: "EYFS", // 02 §4.6 — the milestone framework label (relabel is Phase 5, 07.61)
  chatAttachmentTtlDays: 7, // unlinked chat attachments purged by compact-daily (02 §4.6; 07 §6.2 row 8)
});
