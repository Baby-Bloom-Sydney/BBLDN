> **reference, not spec — foundations win (`CLAUDE.md` Precedence).**

# London pre-read pack (`14.14`) — pointer

The London pre-read pack is superseded by the foundations. Read, from the repo root:

- **`../LDN/SPECS/00-foundations/README.md`** — the file index and ownership matrix (`CLAUDE.md` §1 read 2).

From this file's folder the same path is [`../../../../LDN/SPECS/00-foundations/README.md`](../../../../LDN/SPECS/00-foundations/README.md) (ADR-107: `LDN/` is a sibling checkout of this repo; every worktree symlinks it as `../LDN`).

The original pack (`T-059-london-pitch-materials`: memo, operating model, employment-status research, market research) stays in the Sydney workspace as history; the rulings it carried are ADRs in `../LDN/SPECS/00-foundations/DECISIONS.md`.
