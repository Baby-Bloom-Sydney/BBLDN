# prettier --check . (05 §9 stage 1) — generated, build and reference-only paths are not formatted
node_modules
.next
coverage
playwright-report
test-results
package-lock.json
tsconfig.tsbuildinfo
public/katie-manifest.json
docs/reference/sydney/
CLAUDE.md
.env.test
vercel.json
.env.example
eslint.boundaries.js

# Generated by `supabase gen types typescript --local` (01 §6.2; C-12) and diffed byte-for-byte
# by CI's `types-drift` job against that same command's raw output. Formatting it here would make
# the gate fail on semicolons and line breaks — which is exactly what happened on PR #8's first run.
src/modules/shared-types/database.types.ts
